# Frontend

Dashboard for the AI Revenue Recovery Agent. See the [project README](../README.md)
for setup and how everything fits together.

```bash
npm install
cp .env.example .env
npm run dev   # http://localhost:5173
```

`VITE_API_URL` in `.env` controls where it looks for the backend — defaults to `http://localhost:8000`.
